# Introduction

The easiest way to get familiar with OpenDialog is to start playing around with some example flows or conversational patterns. 

In OpenDialog a conversational pattern is a particular way to take advantage of the different ways conversations can be structured so as to build more robust and reusable conversations. 



