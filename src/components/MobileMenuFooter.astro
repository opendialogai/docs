---
/**
 * Renders nothing: everything this footer holds has been removed.
 *
 * Starlight's version carries the social icons, the theme select and the language
 * select. The site declares no social links, ships light-only so ThemeSelect is
 * blank, and is single-locale so LanguageSelect renders nothing — leaving an empty
 * strip that still drew its own border-top and padding at the foot of the mobile
 * menu, 17px tall, where GitBook shows nothing at all.
 */
---
